#!/usr/bin/env node

import { runGateDriver } from '../lib/driver-runtime.mjs';

await runGateDriver({
  gate: 'cloud-possession-verification',
  tests: ['cloud-possession-proof'],
  scenario: 'cloud-possession-verification',
});
